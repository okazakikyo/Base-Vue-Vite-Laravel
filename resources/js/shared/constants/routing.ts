export const PAGE_NAME = {
    HOME: 'Home',
    LOGIN: 'Login',
} as const;

export const PAGE_PATH = {
    HOME: '/',
    LOGIN: '/login',
} as const;

export const PAGE_TITLE = {
    HOME: 'Home',
    LOGIN: 'Login',
} as const;

export const ROUTE = {
    NAME: PAGE_NAME,
    PATH: PAGE_PATH,
    TITLE: PAGE_TITLE,
}
